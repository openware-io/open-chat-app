name: gv_ui
description: Shared UI foundations, adaptive layout helpers, and reusable presentation primitives for GV Chat.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  flutter:
    sdk: flutter

