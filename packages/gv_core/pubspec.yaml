name: gv_core
description: Core domain, errors, results, extensions, and non-UI foundations for GV Chat.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'
dependencies:
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^2.5.8
  json_serializable: ^6.8.0
